<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Opaque framework theme so the host never shows around the first/last Compose frame. -->
    <style name="Theme.Simula.Interstitial" parent="@android:style/Theme.Material.NoActionBar.Fullscreen">
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:colorBackground">@android:color/black</item>
    </style>
</resources>
